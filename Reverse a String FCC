function reverseString(str) {
  
  return str.split("").reverse().join(""); //there are basic methods of str and array, if these seem unfamiliar, please take your time 
                                           //and go through every single method for str, arr, and numbers on MDN
}

reverseString("hello");  // should return "olleh"
